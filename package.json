{
  "name": "ops-pilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "seed": "npx tsx scripts/seedVectorize.ts",
    "frontend": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "pages:deploy": "wrangler pages deploy frontend/dist"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20250709.0",
    "agents": "^0.24.0",
    "wrangler": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
